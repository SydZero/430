#include <bits/stdc++.h>
using namespace std;

void print(vector<bool>& v){
    for(long long i = 0; i < v.size(); i++){
        cout << v.at(i) << " ";
    }
    cout << endl;
}

#include <bits/stdc++.h>
using namespace std;

int main(){
    long long t;
    cin >> t;
    
    
    for(long long i = 0; i < t; i++){  
        long long n, w, l;
        cin >> n >> w >> l;

        for(int j = 0; j < w; j++){

        }
    
    }
      
    return 0;
}
