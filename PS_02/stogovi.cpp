#include <bits/stdc++.h>
using namespace std;

int main(){

    std::ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int n;

    cin >> n;
    vector<string> strs;
    string temp;
    for(int i = 0; i < n; i++){
        cin >>  temp;
        strs.push_back(temp);
    }


    return 0;
}